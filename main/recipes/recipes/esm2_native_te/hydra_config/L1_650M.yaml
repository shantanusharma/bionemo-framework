defaults:
  - defaults
  - _self_

# Training config
config_name_or_path: nvidia/esm2_t33_650M_UR50D
num_train_steps: 200

dataset:
  micro_batch_size: 4

# WandB config
wandb_init_args:
  name: "esm2_t33_650M_UR50D"
  project: "bionemo-recipes"
  mode: "online"

checkpoint:
  ckpt_dir: "checkpoints/esm2_t33_650M_UR50D_sanity"
